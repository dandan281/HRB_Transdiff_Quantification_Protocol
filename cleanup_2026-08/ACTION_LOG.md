# Cleanup 2026-08 — ACTION LOG (append-only)

Protocol: `C:\Users\liqig\.claude\plans\check-this-handoff-review-giggly-snail.md` Part 5.
Every entry: `#NNN | date | plan item | files touched | command(s) | why | tests before→after | rollback handle`.
Nothing is truly deleted: files move to `cleanup_2026-08/quarantine/<original relative path>`; in-file edits roll back via the per-entry commit. Sealed artifacts (`PrecisionMyotube/runs/`, `annotation_work/bootstrap_v1`, `model_labs/classical/_runs/`, `Conversion_Efficiency/` result folders) are read-only throughout.

---

#001 | 2026-08-12 | Part5 setup | cleanup_2026-08/{baselines,quarantine}/ created | `mkdir -p`; `git count-objects -vH`, `git status --porcelain`, `git for-each-ref`, `git fsck --connectivity-only --unreachable` saved to baselines/ | audit-trail infrastructure required by user before any change | tests n/a (no code touched) | rollback: delete folder (contains only snapshots)
  - Baseline facts: 9,475 loose objects / 21.69 GiB / 0 packs; **9,283 objects unreachable** (aborted `git add` of raw data); 5 `tmp_obj_*` garbage files; HEAD 0322ebf, branch main, remote origin present.

#002 | 2026-08-12 | Tier0-2 | `.gitignore` (root) | Edit: added Cleanup-2026-08 block — Conversion_Efficiency plate result folders, Plate9_C6C2_QTFCs/, CE zips, New_Quantif dbs_cache//myotube//nuclei/ + well-prefixed overlay PNGs + Benchmark myotube*/ renders, `*.npy`, `*.npz`, `tmp/`, `output/`, `model_labs/**/_runs/`, `model_labs/_proof/`, `competitors/MyoFuse/upstream/` | ~9 GB of regenerable results and one nested git clone were un-ignored; a `git add -A` would have committed them (the 21.69 GiB .git is the fossil of exactly that accident) | tests n/a (no code touched) | rollback: checkpoint commit (entry #004)
  - Dry-run verified after edits: would-be-staged set fell from 487 files (incl. ~180 MB of overlay PNGs) to **406 files / 11.7 MB**, all code/docs/small-JSON. Code inside `New_Quantif_*` and `model_labs/` remains tracked; only data subfolders are ignored.

#003 | 2026-08-12 | Tier0-3 | `annotation_tools/.gitignore` | Edit: `qc_review/data/` + `qc_review/models/` → `**/qc_review/data/` + `**/qc_review/models/`; added `.pytest_tmp/` | slash-containing patterns were anchored to `annotation_tools/` and never matched the real paths at `annotation_tools/annotation_tools/qc_review/...` — `accept.csv`/`accept.joblib` were one `git add -A` from being committed against the file's stated intent | tests n/a | rollback: checkpoint commit (entry #004)

#004 | 2026-08-12 | Part5 Step 0 | whole tree | `git checkout -b cleanup-2026-08 && git add -A && git commit` | checkpoint commit — first snapshot of ~48 dirty paths + all untracked code since 0322ebf; every artifact SHA cited in coordination/reports previously bound to an uncommitted tree; user's retrievability requirement | tests: 496 expected baseline (run recorded in #005) | rollback: `git checkout main` (main is untouched; commit is additive on its own branch)

#005 | 2026-08-12 | Tier0-1 | `.git/objects` | `git gc --no-prune` (second attempt with `-c gc.cruftPacks=false`) | pack reachable objects without deleting anything | tests n/a | rollback: nothing deleted; after-state in baselines/git-count-objects_after-gc-noprune.txt
  - EXECUTION NOTES, deviations from the plan text:
    (a) Loose unreachable objects date from **2026-07-13** — already older than git's 2-week default grace, so plain `git gc` would have pruned them IMMEDIATELY. Plan intent (reversible until user re-confirms) therefore required `--no-prune`, not merely "without --prune=now". Used `--no-prune`.
    (b) First `git gc --no-prune` was killed at a 10-min timeout: modern git's default cruft-pack phase was rewriting all 21.6 GiB of unreachable blobs into a cruft pack. Its interrupted `.tmp-40616-pack-*` files (8.5 MB, my own run's leftovers) were removed; rerun with `-c gc.cruftPacks=false` completed in seconds. Logged as an ABORTED-then-retried step, no data affected.
    (c) Outcome: 643 reachable objects → one 8.14 MiB pack; **8,881 loose objects / 21.68 GiB remain on disk, all unreachable, nothing pruned**; 5 stale `tmp_obj_*` garbage files remain.
  - PENDING USER CONFIRMATION: `git prune` (add `git prune --expire=now` semantics are default here since objects exceed grace) would reclaim ~21.6 GB and remove the 5 garbage files. Not run.

#006 | 2026-08-12 | Part5 Step 0 result | branch `cleanup-2026-08` | commit `3113f46` | checkpoint executed: 408 files / 162,668 insertions / ~11.7 MB — all code, docs, coordination contracts, small result-JSONs; content list verified against the dry-run | tests: baseline run launched post-commit (result recorded in #007) | rollback: `git checkout main` (main untouched at 0322ebf)

#007 | 2026-08-12 | Part5 rule 5 | none (read-only) | canonical two-invocation suite per DEVELOPMENT_PLAN §13 (PYTHONPATH=PrecisionMyotube;annotation_tools;model_labs; `pytest PrecisionMyotube/tests` then `pytest annotation_tools/tests model_labs/tests`, both with `--basetemp tmp/...`) | establish the green baseline required before any code change | **57 + 439 = 496 passed, 0 failed** (16 warnings each) | rollback n/a
  - FINDING (new doc bug for the rulings list): the single-line combined command in `HANDOFF_REVIEW_PROMPT_2026-08-12.md` §4 (`pytest model_labs/tests annotation_tools/tests PrecisionMyotube -q`) does NOT reproduce 496 — it yields **342 passed / 154 errors** with garbled cross-suite paths in the error report. The two-invocation §13 procedure is the only verified form. Handoff doc should be corrected.

#008 | 2026-08-12 | Tier0-1 (user-approved via AskUserQuestion) | `.git/objects` | `git prune` | reclaim the unreachable loose objects | tests n/a | rollback: none needed — objects were unreachable duplicates of raw nd2 data still on disk; SHA list preserved in baselines/git-unreachable_before.txt
  - Outcome: **21.69 GiB → 8.15 MiB** (643 packed + 6 loose objects; 5 garbage `tmp_obj_*` removed). After-state in baselines/git-count-objects_after-prune.txt.

#009 | 2026-08-12 | Part3-A1/A2/A3 (user-approved "all three") | `model_labs/omnipose_lab/infer_fold.py`, `model_labs/omnipose_lab/run_folds.py` | Edits | (A1) provenance no longer hardcodes "trained from scratch" — derives from train_manifest `init_model`, and records `init_model`/`init_model_sha256` in the sealed thresholds metadata; (A2) `train_manifest` is now required — missing manifest raises instead of `(train_manifest or {})` silently disarming the held-out leakage assertion; empty `train_wells` also raises; CLI exits with a clear FAIL if `train_manifest.json` is absent; (A3) `false_split_count`/`over_merge_count` added to METRIC_KEYS and as pooled `total_*` (None — never 0 — if any fold lacks the key), so the predeclared T03 primary appears in aggregates | labs suite **439 passed** (tmp/pytest_cleanup_gate1) | rollback: commit recorded below
  - Note: no sealed Omnipose artifact exists yet (zero fold_results locally), so these fixes change no sealed number; they prevent the FIRST sealed run from carrying false provenance.

#010 | 2026-08-12 | Part3-B1..B5 (user-approved "all five") | `Conversion_Efficiency/{myotube_detect,real_fusion,count_well,fusion_index,myotube_sweep,nuclei_filter,length_human_vs_machine}.py` | Edits | (B1) deleted the dead `edge = m ^ binary_closing(...)` line (`binary_closing` unimported → NameError; `edge` unused); (B2) primary gate now appended to `--gates` if missing, mirroring the existing primary-frac guard → no UnboundLocalError at the overlay step; (B3) jsonl writers in count_well/fusion_index/myotube_sweep now replace the well's previous record instead of appending a duplicate (summaries sum every line); (B4) nuclei_filter reads masks from `--outdir` (was hardcoded `plate23_nuclei` input with a free output dir → cross-plate contamination), title derived from the dir, ÷0 guarded; (B5) binpct empty-well row sized `len(EDGES)-1` (was a stale `zeros(3)` from the old 3-bin design → ragged arrays) | all 9 edited files py_compile OK; CE scripts have no test coverage (suite untouched: 439 passed at #009) | rollback: commit recorded below
  - Deliberately NOT touched (sealed-metric changes needing a Codex-lane discussion): `benchmark.py` endpoint_exclusion_um px/µm bug; sum-of-branches vs longest-geodesic length-definition split; `train_fold.py` always-None `failure` field.

#011 | 2026-08-12 | Tier2 (user-approved "quarantine all listed") | 14 files + 2 folders MOVED (never deleted) to `cleanup_2026-08/quarantine/<original relative path>` | `git mv` (tracked) / `mv` (`model_labs/_proof/`, untracked) | dead or superseded code: model_labs `proof_of_loop.py` + `_proof/` + `_shared/train_export.py` (its only consumer), `synth_length_errors.py` (its 2,290 pairs recorded `synthetic_pairs_used: false` in every manifest), `microsam/` placeholder (never run), `omnipose/smoke_test.py` stub; Conversion_Efficiency `cellpose_nuclei.py`, `compare_viz.py`, `conversion_efficiency.py` (watershed route, superseded by Cellpose route), `b02_3d.py`, `b02_conversion.py` (plate/well forks), `fusion_index.py` (superseded by real_fusion; jsonl fix #010 preserved in the quarantined copy), `summary_fusion.py`, `summary_plate.py` (read jsonl files only quarantined writers produced) | import check first: only cross-imports are within the quarantined set (compare_viz→conversion_efficiency, proof_of_loop→train_export); tests reference only the string "microsam", no imports | **full gate 57 + 439 = 496 passed** (tmp/pytest_cleanup_gate2_*) | rollback: `git mv` back (history preserved), or restore from quarantine path
  - NOT quarantined although listed in the plan text: `_shared/smoke.py`/`synthetic.py` (still imported by a live test), `gap_augment.augment_tile()` (in-file dead function — removal is a code edit, deferred with the Part 3A leftovers), `annotation_tools/_launch.err.log` + dual pycache generations (gitignored scratch; deletion needs no quarantine but also wasn't in the approved list).

#012 | 2026-08-12 | Part3-C doc corrections (user: "continue") | 5 docs + 1 docstring | Edits, all as DATED correction notes — never silent rewrites | (C1) training plan §3 initialisation row now says fine-tuned from `bact_phase_affinity` per §5(f), with a correction note; §"original discrepancy" got a dated resolution note ("I have not changed it" was stale); (C3) §6's "requeued roughly every 8–9 hours" + "--requeue make that cost one fold" got a dated falsification note (measured windows 34 s–37 min; --requeue ≠ TIMEOUT); (C2) classical README floor corrected 0.500/0.648/n=40 → **0.8000 / 0.6667 / n=25** in table and prose per DEVELOPMENT_PLAN v2.7 supersession; (new) handoff prompt §4 verify-command replaced with the §13 two-invocation form (the one-liner yields 342/154 errors) + branch-state note; (C6) 374-vs-375 RESOLVED: `corpus_gap_distribution` skips masks <50 px or with failed skeleton profiles, so 374 is the measurement population after guards — documented in `gap_augment.py` docstring, neither count is an error; (Tier0 leftover) MyoFuse NOTICE.md records the upstream/ gitignore decision + re-clone recipe at the pinned SHA `273a2f4d` | tests: gate at #013 | rollback: this commit
  - NOT touched: `coordination/WORKBOARD.md` (integrator-only by contract — staleness items handed to the Codex lane instead).

#013 | 2026-08-12 | Tier1 (low-risk slice) | `Conversion_Efficiency/multinuc_averaged.py`, `model_labs/omnipose_lab/{train_fold,run_folds}.py`, `model_labs/classical/ridge_graph.py` | Edits | (1) receptor canonicalisation unified: multinuc_averaged's exact-match CANON dict deleted in favour of importing `length_human_vs_machine.canon` (verified identical on all 12 known tokens; the two could diverge on the next new token, splitting the multinucleation and length figures); import-tested: `condition_of('23_B02_ctrl')='control'`, `('32_C08_br223_igf1r')='BMPR2 + IGF1R'`; (2) omnipose CLI knobs unified: new `train_fold.add_training_args()`/`config_from_args()` used by BOTH parsers — run_folds' hand-copied block deleted; found live drift in the process: `--autocast` existed on train_fold only (same class as the 16-GPU-hour dataloader drift); both `--help` outputs verified to expose identical training knobs; (3) sum-of-branches vs longest-geodesic length divergence documented at the accumulation site in ridge_graph.py as intentional-while-sealed (reconciliation = Codex-lane decision, behavior deliberately unchanged) | full gate: see below | rollback: this commit
  - DEFERRED with reasons: broad `ce_common.py` refactor of ~13 untested CE scripts (highest new-bug risk in the plan — recommend building smoke tests first; the divergent duplicates, which were the dangerous part, are now unified or documented); `sys.path` bootstrap block (must run before `_shared` is importable — irreducible); `sha256_file` dedupe in classical/ (sealed lane, near-zero benefit); PIXEL_UM single-sourcing (touches sealed classical + shelved models).

#014 | 2026-08-13 | NEW WORK (user: "do conversion efficiency for plate 44") | `Conversion_Efficiency/New_Quantif_P44/` (6 new scripts + README + 4 JSON + 2 figures) | see README | quantify PLATE 44 (`Plate_44 _Tdiffs/`, 40 wells) with the established conversion-efficiency method | n/a (new analysis lane; the 496 suite does not cover Conversion_Efficiency) | rollback: delete the folder — no existing file was modified
  - **Acquisition audit FIRST (read-only, all 40 files): PLATE 44 is not the PLATE_2x format.** ch0/ch2 SWAPPED (ch0=DAPI 429 nm, ch2=AF546 571 nm; Desmin stays ch1); 1818² not 3636²; **1.724571 µm/px not 0.650017** (2.65× coarser, 1.76× field area); 12-bit not 16-bit.
  - **Two silent-corruption traps avoided.** (a) The pipeline's `--nuclei-ch 2` default would have segmented the RECEPTOR channel as nuclei while still producing a plausible Desmin mask — nothing in the output would have looked wrong. (b) `TOPHAT_R = 40` px copied across would have applied a 69 µm top-hat instead of 26 µm. All pixel-valued parameters are now DERIVED from physical size in `p44_layout.py` (top-hat 26 µm → 15 px; ring 10 µm → 6 px), never copied.
  - **Resolution justified, not assumed**: a 50–500 µm² nucleus is 17–168 px here vs 118–1185 px on PLATE_2x, so native-vs-resampled(0.65 µm/px) was measured on the control — 17,759 vs 18,292 valid nuclei = **+3.0%** agreement → native used (5× faster, no interpolation). Recorded in `pilot_cellprob.json`.
  - **Operating point data-driven, uniform**: 5-point cellprob sweep on 4 pilot wells spanning the plate; **all four plateaued at 0 unanimously**; applied unchanged to all 40. Pooled-Otsu Desmin threshold 118.9 raw units from P44's own 739,280 cells. No per-well tuning, no calibration to an expected value (uniform-plate-hyperparameters rule).
  - **RESULT: quantification sound, conversion readout NOT interpretable.** 739,400 valid nuclei / 40 wells. Density confound clean (r=+0.21, p=0.2; cf. P28's 0.99 artifact). But the full spread 9.4–20.3% (2.1×, excluding one dead well) is **smaller than the 4.1× spread Plate 9 measured across its six CONTROL wells alone**, and the assumed control ranks **27/40** (P32's trusted result had its control LOWEST). Max effect 1.29× vs P23's 2.1–2.6×.
  - **Two external blockers, both reported not worked around**: (1) no layout sheet exists for this plate and P44 filenames carry no treatment token — **no condition was invented for any well** (the Plate 9 G09/G10 rule); (2) control identity is a POSITION convention (23_B02 is ctrl on P23/26/28/32), flagged `control_is_assumed_by_position: true`; all absolute per-well numbers are independent of it.
  - **Technical outlier flagged**: 14_B11 (4.5%) has Desmin dbs p99 = 329 against 1,066–2,331 plate-wide — an empty Desmin channel with normal nuclei and background, i.e. a staining/acquisition failure, not biology.
  - Repo hygiene held: 976 MB of masks/dbs_cache correctly ignored by the `New_Quantif_*/{nuclei,dbs_cache}/` rules added in #002; only 374 KB of code + JSON + 2 summary figures staged.

#015 | 2026-08-13 | P44 follow-up (user supplied the plate map, asked for the averaged bar graph) | `New_Quantif_P44/{p44_layout.py,treatment_summary.py,README.md}` + `treatment_summary.{png,json}` | see below | transcribe the layout sheet, average replicate wells, draw the bar graph with error bars | n/a (CE lane) | rollback: this commit
  - **Plate map transcribed with a self-check.** Conditions run in triplicate in READING ORDER and **wrap across rows** — `Alk1` = B11+C02+C03, `C2+Alk1` = C10+C11+D02 — so a row-wise reading would mis-assign 6 wells. 12 conditions ×3 + 2 ×2 = 40, and `p44_layout._selfcheck()` asserts the map covers the imaged wells exactly (no gaps, no extras). Sheet typo `C6+TNFalpa`(E08)/`C6+TNFalpha`(E09) read as one condition and recorded in `SHEET_TYPO_NOTE` rather than silently merged.
  - **Control is `No mb` (B02/B03/B04), n=3.** The pre-map position guess (23_B02) was right but is now read from the sheet; `CTRL_IS_ASSUMED` set False.
  - **VERDICT CORRECTED.** #014 recorded "spread within noise" using **Plate 9's control spread as a stand-in yardstick**, because P44 had no declared replicate structure at that point. With the real replicates: **control is tight — 14.8% ± 0.85 SD, CV 5.7%**, much better than Plate 9. The limiting factor is the opposite of what was provisionally recorded: several *treatment* conditions disagree with themselves (SD up to 3.96 pp; C2 = 9.4/12.2/16.0%) by more than any treatment differs from control. The README headline was rewritten, not patched.
  - **Result: 0 of 13 conditions significant** (Welch t vs control, well as replicate unit, Holm over 13). Added a `separation_vs_controls` statistic (do ALL of a condition's wells fall outside the full control range?) because at n=2–3 it uses no variance estimate and is more informative than a p-value. **C6+TNFalpha is the only condition with complete separation**: 19.0/20.3% vs controls 15.7/14.7/14.1, 1.32×, +5.6 control SD, p_Holm 0.314, n=2 — reported as a follow-up candidate, explicitly not a result.
  - **Figure** built per the dataviz procedure: 2-category color (control vs treatment — hue carries role, never rank; not 14 cycled hues), sheet order not result order, mean ± SEM, individual wells overlaid, control ±1 SD band, n per bar, no value printed on every bar. Rendered and inspected 3× — fixed a control-label/bar collision, a legend marker advertised but never drawn, and red-on-blue label contrast. Excluded well B11 now drawn at its real 4.5% with an × so the exclusion is visible, not merely asserted.
  - Honesty guard kept in both console output and JSON: `power_note` — n=2–3 with 13 comparisons is low-powered; absence of significance is not evidence of absence.

#016 | 2026-08-13 | P44 format parity (user: "the output of New_Quantif_P44 should be like the format of this folder [P32]") | `New_Quantif_P44/{visualize_final.py,density_confound.py,README.md}` + 40 overlays + `conversion_summary_bar.png` + `percell_values_r10.0.npz` + 2 JSON | see below | bring P44's artifact set to parity with `New_Quantif_P32` | n/a (CE lane) | rollback: this commit
  - Added the missing P32-equivalents: `visualize_final.py` → 40 × `<well>_percell_classified.png` spatial overlays (same colour code), `conversion_summary_bar.png` (P32 form: control first, treated ascending, %/fold bold labels, dashed control line) and `visualize_final.json`; `percell_values_r10.0.npz` per-cell cache; `density_confound.py` → `density_confound.png`/`.json` reading `visualize_final.json` exactly as P32's does.
  - Two deliberate deviations from P32's form, both forced by the data and documented in-file: (a) geometry from `p44_layout`, never `real_fusion.UM` (the PLATE_2x constant); (b) the summary bar is **per condition with SEM error bars and per-well dots** because P44 has replicate wells, where P32 had one well per condition.
  - **Independent cross-check passed**: `visualize_final.py` recomputes conversion by a separate code path from `percell_desmin.py` and every one of the 40 wells matches to the reported decimal. Spatial overlays are also coherent — magenta Desmin+ nuclei track along green fibres — which is independent visual evidence the ch0/ch1 mapping is right.
  - Density confound in P32's own form: well-level r = **+0.111** (p=0.50, n=39), condition-level r = +0.258. Cleaner than the +0.207 in #014, which included the B11 technical failure.
  - Fixed a label-placement bug inherited from P32's chart code: its `+0.4` offset assumes no error bars or replicate dots, so labels collided. P44's version places each label above whichever is higher, the SEM cap or the topmost replicate dot.
  - **One artifact deliberately NOT produced: multinucleation.** It needs myotube ridge masks, and `detect_myotubes` uses Sato `sigmas=(1,2,4)` px at 0.650 µm/px = ridge scales ~1.3–5.2 µm, which at P44's 1.725 µm/px are **0.75–3.0 px** — at or below one pixel. Fibre tracing would systematically miss thin tendrils and undercount multinucleation while looking fine. Conversion is unaffected (nuclei ~14 µm ≈ 8 px, confirmed by the +3.0% resample cross-check). Stated in the README before running rather than after.
  - Tracking parity holds: the 40 overlays and the `.npz` are gitignored by the same rules that ignore P32's, so only 252 KB of scripts/JSON/figures staged.

#017 | 2026-08-13 | P44 troubleshooting (user: "weird results... might be noise from the desmin artifacts") | 5 new read-only diagnostic scripts + README correction | see below | find why P44's conversion readout behaves oddly | n/a (CE lane; all scripts read-only, no result overwritten) | rollback: this commit
  - **ROOT CAUSE FOUND — Desmin ANTIBODY STAINING varies up to 2.19× across wells (1.60× between biologically identical replicates), and the fixed absolute threshold converts that gain directly into apparent conversion.** Not repairable in software.
  - Evidence chain, cheapest test first: (1) **empirical null** — B11 has effectively no Desmin yet 4.5% of its cells score positive → false-positive floor every well pays; (2) background drift r=+0.07 and saturation 0.0000% both **excluded**; (3) **the operator's debris hypothesis is real but marginal** — in control wells only 47% of above-threshold pixels are fibre-like (31% blobby, 22% sub-resolution specks), yet despeckling to 200 µm² moved the floor only 4.51→4.10% and replicate SD 2.03→1.91; (4) **within-condition r(Desmin brightness, conversion) = +0.917** (p=3e-16), explaining **84%** of replicate scatter with biology held constant; (5) partial correlations separate gain from abundance — brightness|coverage-held **r=+0.830**, so it is technical gain and not merely "more myotube"; (6) **r(DAPI p99, Desmin p99) = +0.045** → the gain is antibody-specific, NOT optical/exposure, so no loading-control normalisation is available.
  - **A fix was implemented, tested and REJECTED on evidence.** Per-well gain normalisation removes the artifact cleanly (brightness r +0.917→−0.06; replicate SD −62%) but scores the **empty B11 null well at 98%**, because dividing by a tiny p99 inflates everything — gain and abundance are inseparable from one channel. Same failure mode as this project's earlier flat-fold-change per-image-normalisation bug; the pre-declared null-well criterion is what caught it. Documented as tried-and-rejected rather than quietly dropped.
  - **Structural finding**: the pooled per-cell distribution is unimodal with a shoulder, not bimodal. The Otsu cut sits where the histogram is still **11% of peak height** — inside the background's own right flank — and ±10% of threshold moves plate conversion by **16% relative**. No threshold separates two populations because there are not two populations at this signal quality.
  - **Two prior conclusions CORRECTED in the README, not patched over**: the ranking is not usable (~83% of the between-condition spread, 5.9 of 7.1 pp, is reproducible from staining brightness alone), and **the C6+TNFalpha candidate from #015 is WITHDRAWN** — it has the plate's highest condition-mean staining brightness, and ≈2.6 of its 4.8 pp apparent effect is attributable to staining at the measured slope of 7.33 pp per 1,000 p99 units.
  - Nuclei counts are explicitly unaffected (artifact confined to the Desmin channel; DAPI segmentation independently validated by the +3.0% resample cross-check). Also noted: DAPI p99 hits the 12-bit ceiling (4095) in several wells, so DAPI is partly saturated and would be a poor loading control even if it did track Desmin.
  - Recommendation recorded: re-stain all 40 wells from one matched master mix at fixed exposure. Everything else is mitigation.

#018 | 2026-08-13 | P44 normalised bar graph (user: "show me the bar graph after you have normalized the wells") | `New_Quantif_P44/treatment_summary_normalised.py` + `.png`/`.json`, README section | see below | render the gain-normalised treatment figure the user asked to see | n/a (CE lane) | rollback: this commit
  - Figure built in the same layout as `treatment_summary.png` (sheet order, mean ± SEM, per-well dots, control ±1 SD band) but on gain-normalised values, with a second panel showing the disqualifying null-well check and a red NOT-ADOPTED caption baked into the image so it cannot be mistaken for a validated result if the PNG travels without the conversation.
  - Normalised numbers: control 14.55% (SD 0.51); replicate SD 2.03 → **0.77 pp** across conditions (C6+TNFalpha's two wells now agree to 0.13 pp); staining correlation r +0.917 → −0.06. **Still 0 of 13 significant** after Holm.
  - **New observation, logged as a hypothesis not a result**: after normalisation the four highest conditions are all C6-containing. Pooling by the plate's own backbone factor gives C6-containing (n=14 wells) 16.62% vs C2-containing (n=12) 15.32%, +1.30 pp, 1.08×, **Welch p = 0.017** — where the identical contrast on the absolute threshold gives **p = 0.42**, i.e. the staining noise was masking it. Flagged in the README as post-hoc, uncorrected, and resting on a method that fails its own null-well criterion; offered as a cheap specific thing to test on a re-stained plate.
  - Layout bugs found by rendering and inspecting twice: a right-panel title colliding with the main title (moved inside the axes), and the red caption clipped by the bar (shortened + repositioned). `tight_layout` replaced with explicit `subplots_adjust` since the mixed-scale panels are not tight_layout-compatible.
