# T02 immediate model implementation handoff

- **Request ID:** `2026-07-21-t02-start`
- **From lane:** Codex integration/core
- **To lane / owner:** Claude model laboratories
- **Related task IDs:** T01, T02, T03
- **Target paths:** `model_labs/omnipose/**`, `model_labs/_shared/**`, and new T02-owned model code

## 1. Authorization and input

The project owner explicitly removed G-SO1 from the development critical path. Begin exploratory
candidate implementation immediately from:

`PrecisionMyotube/annotation_work/bootstrap_v1/bootstrap_manifest.json`

Manifest SHA-256:
`44e381142ca31ef650d202f7d2edbb53895602a13b3e4f6959437504d667de94`

The version contains 375 real complete masks across six wells, 40 real correction pairs, and 2,290
eligible synthetic pairs. Twelve synthetic derivatives of two excluded masks have already been
removed. All evidence remains single-operator and proposal-conditioned.

## 2. Required observable behavior

1. Implement a reproducible classical ridge/graph candidate as the floor.
2. Implement the real Omnipose training/inference harness in its isolated environment.
3. Use whole-well leave-one-well-out folds; never split object crops from one well across train and
   validation.
4. Use synthetic pairs only for pretraining/augmentation. Keep the 40 real corrections separate
   for real-error refinement evaluation and report if any are used for tuning.
5. Export every prediction through the canonical unreviewed `InstanceSet` adapter with model,
   environment, input-data, fold, seed, and threshold hashes.
6. Label every result exploratory; do not claim consensus, prospective validation, or inter-rater
   agreement.

## 3. Acceptance checks

- [ ] Both candidate commands run from documented isolated environments.
- [ ] Six sealed development folds are produced for each candidate.
- [ ] No well or correction-pair leakage is present.
- [ ] Predictions validate against the canonical schema and remain `reviewed=false`.
- [ ] A compact run manifest records commands, hashes, seeds, timing, and failures.
- [ ] T03 receives predictions without model-selection changes after seeing a held-out fold.

## 4. Resolution

- Owner commit:
- Tests:
- Codex read-only verification:
